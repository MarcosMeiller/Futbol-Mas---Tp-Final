import { EventIconDirective } from './event-icon.directive';

describe('EventIconDirective', () => {
  it('should create an instance', () => {
    const directive = new EventIconDirective();
    expect(directive).toBeTruthy();
  });
});
